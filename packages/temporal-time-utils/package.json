{
  "name": "temporal-time-utils",
  "version": "0.0.1",
  "author": "sw-yx",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "files": [
    "dist",
    "."
  ],
  "engines": {
    "node": ">=16"
  },

  "scripts": {
    "build": "tsc --build",
    "build.watch": "tsc --build --watch",
    "lint": "eslint ."
  },
  "dependencies": {
    "cron-parser": "^4.2.1",
    "date-fns": "^2.14.0"
  },
  "devDependencies": {
    "config": "*",
    "temporalio": "*",
    "tsconfig": "*",
    "typescript": "^4.5.3"
  }
}
